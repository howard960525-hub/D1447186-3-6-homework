[project]
name = "typing-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
]

[tool.uv.workspace]
members = [
    "typing_game",
]
